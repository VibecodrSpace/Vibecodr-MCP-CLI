FROM docker.io/cloudflare/sandbox:0.10.0-python

# The Sandbox SDK docs require the image tag to match the installed
# @cloudflare/sandbox package version. The python variant keeps the first paid
# alpha useful for both JS/TS and Python-oriented agent test runs.
